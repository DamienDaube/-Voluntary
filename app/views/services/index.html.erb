
<div class="container">
  <h1 id="main-list-title">Services</h1>
  <div class="row">
    <% @services.each do |service| %>
      <div class="col-xs-12 col-sm-4">
          <div class="card-description">
            <h2><%= link_to service.category, services_path(service) %> </h2>
          </div>
      </div>
    <% end %>
  </div>
</div>
