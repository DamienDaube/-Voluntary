<div class="container_services">
  <div class="row">
    <% @categories.each do |category| %>
      <div class="col-md-6 col-md-offset-1">
          <div class="list-description">
            <h2><%= link_to category, services_path(category: category) %> </h2>
          </div>
      </div>
    <% end %>
  </div>
</div>
